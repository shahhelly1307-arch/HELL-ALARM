// Timer panel — placeholder for future timer-specific overlay content.
// Static Figma elements (STUDY TIME, 25:00, START) are rendered by App.tsx's
// figma-text loop and routed to timer via name-based classification.
// When timer countdown state is wired up, dynamic count/status will replace those labels.
export function TimerPanel(): React.JSX.Element | null {
  return null
}